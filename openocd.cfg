# OpenOCD 配置文件 — J-Link + STM32F405
# 使用方法: openocd -f openocd.cfg

# 调试器接口
source [find interface/jlink.cfg]
transport select swd

# 目标芯片
source [find target/stm32f4x.cfg]

# 复位配置
reset_config srst_only srst_nogate
